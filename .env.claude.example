# =========================================================
# Claude runner sidecar env
# Used only by the `claude-runner` service.
# =========================================================

# Sidecar API token (must match deepbot's CLAUDE_SUBAGENT_SIDECAR_TOKEN)
CLAUDE_RUNNER_TOKEN=change-this-sidecar-token

# Timeout for one claude run inside sidecar (milliseconds)
CLAUDE_RUNNER_TIMEOUT_MS=300000

# Route Claude Code through LiteLLM (Anthropic-compatible endpoint)
CLAUDE_CODE_ANTHROPIC_BASE_URL=http://litellm:4000
CLAUDE_CODE_ANTHROPIC_AUTH_TOKEN=change-this-litellm-key
